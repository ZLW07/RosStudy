<launch>
    <!-- 设置参数 -->
    <param name="robot_description" textfile="$(find TestInput)/urdf/ShowModel.urdf" />

    <!-- 启动 rviz -->
    <node pkg="rviz" type="rviz" name="rviz"  args="-d $(find TestInput)/config/uNavBrainModel_1.rviz"/>
</launch>